var winState = function() {};

winState.prototype ={
    /*init: function(time,score,lives,bonus){
        stateText = game.add.text(5,505,'Time: '+time+'\t\t\tScore: '+score+'\t\t\tLives lost: '+lives+'\t\t\tBonuses Collected: '+bonus,{font: "Bold 16px serif",fill:"yellow"});
    },
    create: function(){
        victorySound = game.add.audio('win');
        victorySound.play();
        game.add.sprite(0,0,'victory');
    },*/
};